namespace EventManagement.InputManager.Events.EventTypes
{
    public enum EventType
    {
        OnKey,
        OnKeyUp,
        OnKeyDown,
        OnMouse,
        OnMouseUp,
        OnMouseDown,
    }
}
